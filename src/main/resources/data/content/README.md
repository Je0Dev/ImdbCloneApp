# `resources/data/content/` — Content Data Files

Files:
- `movies_updated.txt` — Movie data (title, year, genre, cast, director, etc.)
- `series_updated.txt` — Series data (title, seasons, episodes, cast, etc.)
