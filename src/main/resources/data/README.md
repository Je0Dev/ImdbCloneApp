# `resources/data/` — Data Files

Contains the raw data files (TXT format) that are parsed at application startup to populate the in-memory data store.

Sub-directories:
- `content/` — Movie and series data files
- `nominations/` — Awards and box office data
- `people/` — Actor, director, and user data files
