# `src/main/java/` — Java Source Root

All Java source code for the application is rooted here under `com/papel/imdb_clone/`. Contains the module descriptor (`module-info.java`) and all application packages.
