# `model/` — Domain Models

Contains all domain model classes organized by domain area.

Sub-packages:
- `content/` — Content models (Movie, Series, Episode, Season)
- `people/` — People models (Actor, Director, Celebrity, User)
- `rating/` — Rating models (Rating, UserRating)
