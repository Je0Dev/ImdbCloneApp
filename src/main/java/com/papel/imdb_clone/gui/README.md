# `gui/` — JavaFX Application Class

Contains `MovieAppGui.java`, the main JavaFX `Application` subclass that initializes the primary stage, loads the initial FXML view, and starts the application UI lifecycle.
