# `config/` — Application Configuration

Contains the `ApplicationConfig.java` class which manages application-wide configuration settings such as file paths, data directories, and runtime parameters.
