# `service/data/` — Data Loading Services

Contains the data loading framework that reads and parses data from text files.

Sub-packages:
- `base/` — Base data loading framework
- `loader/` — Specific data loader implementations
- `loader/content/` — Content-specific loaders
- `loader/people/` — People-specific loaders
