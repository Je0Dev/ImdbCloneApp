# `service/search/` — Search & Service Locator

Files:
- `SearchService.java` — Search logic across all content types
- `SearchCriteriaHelper.java` — Search criteria builder
- `ServiceLocator.java` — Central service registry (dependency injection)
