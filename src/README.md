# `src/` — Source Code Root

Root directory for all source code and resources.

- `main/` — Main application source and resources
- `test/` — Test sources (if present)

The application follows the **MVC (Model-View-Controller)** pattern with a **Service Locator** for dependency management.
