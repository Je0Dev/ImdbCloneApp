# `data/` — External Data Storage

Contains serialized user data (`user_data.ser`) persisted between application sessions. This is the runtime data storage used by the `UserStorageService` to save and restore user accounts and their associated data.
